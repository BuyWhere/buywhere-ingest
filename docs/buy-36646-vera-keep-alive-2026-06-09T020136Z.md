# BUY-36646 — Vera sustained throughput keep-alive heartbeat (2026-06-09T02:01:36Z)

Routine execution issue for the 5-minute `BUY-30854` lane keep-alive watchdog.

## Commands

```bash
bash -n scripts/buy30854-lane-keep-alive.sh
bash scripts/buy30854-lane-keep-alive.sh
tail -n 8 logs/buy30854_keep_alive.log
ps -eo pid,ppid,etimes,cmd | rg 'buy30590-deep-page-loop|buy30331-sustained-loop|buy30590-woocommerce-discover|buy30727-lane-supervisor'
cat data/buy30854-keep-alive-state.json
python3 - <<'PY'
import json
from pathlib import Path
p = Path('data/buy30854-keep-alive-escalation.json')
obj = json.loads(p.read_text()) if p.exists() else {'escalations': []}
arr = obj.get('escalations', [])
print(f'count={len(arr)}')
if arr:
    import json as _json
    print(_json.dumps(arr[-1], indent=2))
PY
```

## Result

- `bash -n scripts/buy30854-lane-keep-alive.sh` passed.
- The explicit heartbeat tick completed at `2026-06-09T02:01:36Z`.
- `deep_page_loop` stayed healthy at pid `2778633`.
- `sustained_loop` stayed healthy at pid `2691392`.
- `lane_supervisor` remained intentionally skipped because `data/buy30727-supervisor.stopped` is present per `BUY-31452`.
- `woocommerce_discover` was not restarted because the completion checkpoint `data/checkpoints/buy30590_woocommerce.completed` remains present, with dead-count state still `2`.
- No new escalation was emitted by this tick; the escalation file still ends with the historical `deep_page_loop` entry from `2026-06-08T21:21:49Z`.

## Evidence

Latest keep-alive log block:

```text
===== keep-alive tick 2026-06-09T02:01:36Z =====
[2026-06-09T02:01:36Z] deep_page_loop OK pid=2778633
[2026-06-09T02:01:36Z] sustained_loop OK pid=2691392
[2026-06-09T02:01:36Z] lane_supervisor SKIPPED (BUY-31452 stop marker present; see data/buy30727-supervisor.stopped)
[2026-06-09T02:01:36Z] keep-alive tick complete
```

Tracked processes after the tick:

```text
2691390       1   18233 bash -c node scripts/buy30331-sustained-loop.mjs & wait
2691392 2691390   18233 node scripts/buy30331-sustained-loop.mjs
2778630       1   16789 bash -lc exec 9>&-; node scripts/buy30590-deep-page-loop.mjs & wait
2778633 2778630   16789 node scripts/buy30590-deep-page-loop.mjs
```

State file after the tick:

```json
{
  "deep_page_loop": 0,
  "sustained_loop": 0,
  "woocommerce_discover": 2
}
```

Latest escalation entry remains unchanged:

```json
{
  "lane": "deep_page_loop",
  "dead_ticks": 8,
  "at": "2026-06-08T21:21:49Z",
  "note": "lane DEAD on >=4 consecutive keep-alive ticks; escalate to parent BUY-30854 with diagnostic context"
}
```
