# BUY-35544 — Vera sustained throughput keep-alive tick (2026-06-08T12:15Z)

5-min heartbeat fire (routine 476009cc). One tick of `bash scripts/buy30854-lane-keep-alive.sh`.

## Tick result

Tick at `2026-06-08T12:15:32Z`. Script exit 0.

- `deep_page_loop` OK pid=4044040 (5h42m uptime, no restart)
- `sustained_loop` OK pid=466411 (3h9m uptime, BUY-35231 `& wait` parent wrapper keeps it past the 5-min reaper)
- `lane_supervisor` SKIPPED (BUY-31452 stop marker at `data/buy30727-supervisor.stopped`)
- `woocommerce_discover` SKIPPED (completion marker at `data/checkpoints/buy30590_woocommerce.completed`)

No restarts. No escalations. `data/buy30854-keep-alive-escalation.json` does not exist.

State: `{"deep_page_loop": 0, "sustained_loop": 0, "woocommerce_discover": 2}` — the 2 is a stale pre-marker value (script no longer touches it).

## Why this issue is `done`

The keep-alive tick is the unit of work for BUY-35544. Both in-scope lanes (deep_page + sustained) are healthy and require no restart. The 5-min fire cadence continues via routine 476009cc per BUY-35528. No follow-up needed.

## Script integrity

`scripts/buy30854-lane-keep-alive.sh` (6734 bytes, 254 lines) carries all 8 accumulated fixes from the BUY-3xxxx keep-alive series:
- BUY-34462 self-loop (no bash wrapper restart)
- BUY-35030 pushd / setsid / explicit disown / exit 0
- BUY-35210 wrapper pgrep filter (Claude shell-snapshot)
- BUY-35231 `& wait` reaper survival
- BUY-35280 optional `/usr/bin/` prefix on bash filter
- BUY-31716 keep-alive filter rule
- BUY-34140 NOPASSWD structural fix (not needed for this script)
- BUY-30854 do_wait hang (resolved by BUY-35030 pattern)

## Throughput context

Last dispatcher check (12:09:48Z) — `last_check_result: PASS`, 350,699 rows (233.8% of 150K/hr target). Strong recovery from BUY-35444 3rd maglev restart. Writer exited recovery mode.
